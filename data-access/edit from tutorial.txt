sumber:
- https://go.dev/doc/tutorial/database-access, 
- https://go.dev/doc/articles/wiki/

jalanin file SQL:
- asli = source /path/to/create-tables.sql
- baru = source C:/Data Stevan/Latihan/golang/data-access/create-tables.sql;
Ganti username dan password:
- PS C:\Data Stevan\Latihan\golang\data-access> set DBUSER=user    
- PS C:\Data Stevan\Latihan\golang\data-access> set DBPASS=root

Sebelum go run:
- $env:DBUSER="root"
- $env:DBPASS="root"

buka mysql dari cmd: mysql -u root -p
jalanin pakai page :
$ go build main.go
$ ./ main

gabungan:
cd "c:\Data Stevan\Latihan\golang\data-access"; $env:DBUSER="root"; $env:DBPASS="root"; go run main.go

kill process:
 Get-Process | Where-Object {$_.Name -like "*go*"} | Stop-Process -Force -ErrorAction SilentlyContinue; Get-NetTCPConnection -LocalPort 8080 -ErrorAction SilentlyContinue | ForEach-Object {Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue}